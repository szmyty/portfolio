export type LoadingStateProps = {
  /** Accessible label for the loading indicator. Defaults to "Loading…". */
  label?: string;
  /** Supporting text rendered below the spinner. */
  description?: string;
  /** Additional class names applied to the root wrapper. */
  className?: string;
};
