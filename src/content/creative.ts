import type { CreativeWork } from "./types";

/**
 * All creative works.
 *
 * Add entries here when a creative work is ready to be surfaced in the
 * portfolio. Each entry must conform to the {@link CreativeWork} interface
 * defined in `./types`.
 */
export const creativeWorks: CreativeWork[] = [];
