import type { Project } from "./types";

/**
 * All software projects.
 *
 * Add entries here when a project is ready to be surfaced in the portfolio.
 * Each entry must conform to the {@link Project} interface defined in
 * `./types`.
 */
export const projects: Project[] = [];
